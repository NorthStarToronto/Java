package Core4;

/* Save the original array into another variable
   => a new initialized array with increased size
   => loop through the original array and copy each element to the new array
 */
public class ResizeArray {
}
